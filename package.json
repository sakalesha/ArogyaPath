{
  "name": "arogyapath",
  "version": "1.0.0",
  "description": "ArogyaPath: Clinical Chatbot with NLP, Pathways, and Hospital Discovery",
  "scripts": {
    "install:frontend": "npm install --prefix frontend",
    "install:backend": "pip install -r backend/nlp/requirements.txt",
    "install:all": "npm run install:frontend && npm run install:backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:backend": "cd backend/nlp && uvicorn main:app --host 0.0.0.0 --port 8001 --reload",
    "dev": "npx concurrently \"npm run dev:frontend\" \"npm run dev:backend\"",
    "start": "npm run dev"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
